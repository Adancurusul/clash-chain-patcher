# Interval 输入框显示修复

**问题**: Interval 输入框显示 "Your" 而不是默认值 "5"

**版本**: 0.1.2
**日期**: 2026-02-03

---

## 🐛 问题描述

**现象**:
```
Interval: [Your] min
          ↑
       显示了占位符文字
```

**预期**:
```
Interval: [5] min
          ↑
      显示默认间隔
```

---

## 🔧 修复内容

### 1. 添加 empty_text 属性

**位置**: `src/app.rs:381-387`

```rust
interval_input = <TextInput> {
    width: 50,
    height: 24,
    text: "5"
    empty_text: "5"  // ← 新增：空白时显示的文字
    draw_text: {color: #ffffff, text_style: {font_size: 9.0}}
    draw_bg: {color: #555555}
}
```

---

### 2. 启动时设置默认值

**位置**: `src/app.rs:847-849`

**修复前**:
```rust
// Load auto check interval from text input
self.state.auto_check_interval = self.ui.text_input(id!(interval_input))
    .text()
    .parse::<u64>()
    .unwrap_or(5);
```

**修复后**:
```rust
// Set default interval
self.ui.text_input(id!(interval_input)).set_text(cx, "5");
self.state.auto_check_interval = 5;
```

**改进**:
- ✅ 直接设置文本为 "5"
- ✅ 直接设置状态为 5
- ✅ 不依赖解析（更可靠）

---

## ✅ 修复结果

**启动后**:
```
Interval: [5] min
          ↑
      正确显示数字
```

**可以修改**:
```
1. 点击输入框
2. 输入新数字（如 10）
3. 点击 Auto: OFF → ON
4. 使用新间隔（10 分钟）
```

---

## 📊 编译状态

```bash
$ cargo build --release
   Compiling clash-chain-patcher v0.1.2
    Finished `release` profile [optimized] target(s) in 2m 00s
```

**结果**:
- ✅ 0 errors
- ✅ 0 warnings
- ✅ 编译成功

---

## 🧪 测试步骤

```bash
# 1. 运行应用
cargo run --release

# 2. 检查 Proxy Pool 区域
应该显示：
  Interval: [5] min
  而不是：
  Interval: [Your] min

# 3. 测试修改间隔
- 点击输入框
- 输入 "10"
- 开启自动检测
- 应该使用 10 分钟间隔
```

---

## 💡 技术说明

### Makepad TextInput 属性

| 属性 | 用途 | 示例 |
|------|------|------|
| `text` | 初始文本值 | `text: "5"` |
| `empty_text` | 空白时的占位符 | `empty_text: "5"` |

**区别**:
- `text` - 实际值，可以被清空
- `empty_text` - 占位符，只在空白时显示

**最佳实践**:
- 两者都设置相同的值
- 确保用户知道默认值是什么

---

## 📝 修改文件

**src/app.rs**:
- 第 384 行：添加 `empty_text: "5"`
- 第 847-849 行：启动时设置默认值

---

**修复完成！** ✅

现在 Interval 输入框会正确显示默认值 "5"，用户可以清楚看到当前设置的间隔时间。
